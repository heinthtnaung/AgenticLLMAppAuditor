# Collect this project's suite and nothing else. Without `testpaths`, a bare
# `pytest` at the repo root also walks `fetched/` -- the third-party
# repositories src/fetch_repo.py downloads, each with its own tests and its own
# uninstalled dependencies -- and dies in collection before running ours.
[pytest]
testpaths = tests
