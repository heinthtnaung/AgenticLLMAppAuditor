# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` is NOT ignored, against the scaffold's default and on purpose.
# The built page is what Python serves, so committing it is what makes
# "clone, pip install, run" true without Node. Rebuild it with
# `npm run build` in the same change as any edit under `src/`.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
