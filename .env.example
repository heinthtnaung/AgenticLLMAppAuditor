# The local model server's settings. Copy to `.env` and change what you need;
# an environment variable of the same name wins over the file. Only these four
# AUDITOR_* keys are read, and a misspelt one is refused.
#
# A council still runs only with --council-member: AUDITOR_MODEL is the model a
# measurement asks when it is not told one. The tests never read this file; the
# live test asks another model through COUNCIL_LIVE_MODEL.
AUDITOR_MODEL=qwen2.5:7b-instruct
# Must be this machine. The old form ending in /api/generate is also read.
AUDITOR_SERVER_URL=http://127.0.0.1:11434
AUDITOR_TIMEOUT_SECONDS=180
AUDITOR_CONTEXT_TOKENS=8192
